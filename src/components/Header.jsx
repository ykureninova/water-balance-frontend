import React from "react";

const Header = () => {
  return (
    <header className="header">
      <span className="logo">drops.</span>
      <button className="menu">☰</button>
    </header>
  );
};

export default Header;
